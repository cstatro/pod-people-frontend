const defaultState = {
  user: { id: 1 }
};

export const userState = (state = defaultState, action) => {
  switch (action.type) {
    // case value:
    //   break;

    default:
      return state;
  }
};
